---
phase: 02.1-mapbox-migration
plan: 04
type: execute
wave: 3
depends_on: ["02.1-02", "02.1-03"]
files_modified:
  - HouseFriend/Views/HFMapView.swift
  - HouseFriend/ContentView.swift
  - HouseFriend/Views/CrimeTileOverlay.swift
  - HouseFriend/Views/NoiseSmokeRenderer.swift
  - HouseFriend/Services/CrimeService.swift
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-07
  - MAP-08
  - MAP-09
  - MAP-10
must_haves:
  truths:
    - "App builds without CrimeTileOverlay.swift and NoiseSmokeRenderer.swift"
    - "No MapKit overlay or renderer code remains in HFMapView.swift"
    - "All 10 data layers display correctly on the Mapbox map"
    - "Tap and long-press gestures work for all layers"
    - "Crime layer uses dark tiles, other layers use light tiles"
    - "Population ZIP tap uses Mapbox layer query instead of ray-casting"
  artifacts:
    - path: "HouseFriend/Views/HFMapView.swift"
      provides: "Complete Mapbox-based map view with all layers"
      contains: "import MapboxMaps"
    - path: "HouseFriend/ContentView.swift"
      provides: "ContentView wired to Mapbox HFMapView with all callbacks"
      contains: "mapViewport"
  key_links:
    - from: "HouseFriend/ContentView.swift"
      to: "HouseFriend/Views/HFMapView.swift"
      via: "All layer data arrays + callbacks"
      pattern: "HFMapView\\("
    - from: "HouseFriend/Services/CrimeService.swift"
      to: "HouseFriend/Views/HFMapView.swift"
      via: "crimeHotspots array feeds HeatmapLayer source"
      pattern: "crimeHotspots"
---

<objective>
Final wiring, cleanup, and verification of the Mapbox migration. Delete the old MapKit renderer files (CrimeTileOverlay.swift, NoiseSmokeRenderer.swift), clean up any remaining MapKit overlay code in HFMapView, ensure all ContentView callbacks and data flows work end-to-end, and perform visual verification of all 10 layers.

Purpose: Complete the migration by removing dead code, fixing any wiring gaps, and visually confirming all layers work.
Output: Clean, fully-migrated Mapbox map with all 10 layers verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-mapbox-migration/02.1-RESEARCH.md
@.planning/phases/02.1-mapbox-migration/02.1-01-SUMMARY.md
@.planning/phases/02.1-mapbox-migration/02.1-02-SUMMARY.md
@.planning/phases/02.1-mapbox-migration/02.1-03-SUMMARY.md
@HouseFriend/Views/HFMapView.swift
@HouseFriend/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old renderer files, clean up MapKit references, fix remaining wiring</name>
  <files>
    HouseFriend/Views/CrimeTileOverlay.swift,
    HouseFriend/Views/NoiseSmokeRenderer.swift,
    HouseFriend/Views/HFMapView.swift,
    HouseFriend/ContentView.swift,
    HouseFriend/Services/CrimeService.swift
  </files>
  <read_first>
    HouseFriend/Views/HFMapView.swift,
    HouseFriend/ContentView.swift,
    HouseFriend/Views/CrimeTileOverlay.swift,
    HouseFriend/Views/NoiseSmokeRenderer.swift,
    HouseFriend/Services/CrimeService.swift
  </read_first>
  <action>
    **Step 1: Delete old renderer files.**
    - Delete `HouseFriend/Views/CrimeTileOverlay.swift` (177 lines)
    - Delete `HouseFriend/Views/NoiseSmokeRenderer.swift` (83 lines)
    - These are replaced by Mapbox HeatmapLayer and stacked LineLayer respectively.
    - Since Xcode 16 uses `PBXFileSystemSynchronizedRootGroup`, simply deleting the files removes them from the target.

    **Step 2: Update CrimeService.swift.**
    The `CrimeTileOverlay.Hotspot` struct is used as the data type for crime hotspots. Since we deleted CrimeTileOverlay.swift, move the `Hotspot` struct into CrimeService.swift:
    ```swift
    /// Hotspot derived from real crime data -- coordinate + weight
    struct CrimeHotspot {
        let lat: Double
        let lon: Double
        let weight: Double  // 0.0-1.0
    }
    ```
    - Rename all references from `CrimeTileOverlay.Hotspot` to `CrimeHotspot`
    - Move the `buildHotspots(from:)` static method from CrimeTileOverlay to CrimeService
    - Update `@Published var hotspots: [CrimeTileOverlay.Hotspot]` to `@Published var hotspots: [CrimeHotspot]`
    - Update HFMapView.swift: change `crimeHotspots` property type from `[CrimeTileOverlay.Hotspot]` to `[CrimeHotspot]`

    **Step 3: Clean up HFMapView.swift.**
    - Ensure no `import MapKit` remains in this file
    - Ensure no `MKMapView`, `MKOverlay`, `MKAnnotation`, `MKPolygon`, `MKPolyline`, `MKTileOverlay`, `UIViewRepresentable`, or `Coordinator` references remain
    - Ensure no `NoiseSmokeRenderer` or `CrimeTileOverlay` references remain
    - Remove the `Array safe subscript` extension if it was only used by the old overlay parsing code
    - Verify all layer data properties have correct types and match ContentView's passing

    **Step 4: Fix ContentView wiring.**
    - Verify all callback closures compile and run correctly with the new Mapbox types
    - Verify the ZIP tap flow works: Mapbox layer tap query -> extract zipId -> find ZIPCodeRegion -> call existing handler
    - Verify `crimeService.hotspots` type matches what HFMapView expects after the Hotspot rename
    - Ensure `onNoiseFetchCancel` is still wired (Mapbox doesn't fire regionWillChange the same way -- may need to wire differently or keep as-is)
    - Check that the `overrideUserInterfaceStyle = .dark` line is completely gone (replaced by `.standard(lightPreset: .night)` in HFMapView)
    - Remove any now-unused `import MapKit` if MapKit is truly no longer needed in HFMapView.swift (it IS still needed in ContentView.swift for MKLocalSearch and in services)

    **Step 5: Grep-verify cleanup.**
    Search the entire project for any remaining references to deleted types:
    - `NoiseSmokeRenderer` -- should only appear in git history
    - `CrimeTileOverlay` -- should only appear in git history
    - `MKTileOverlay` -- should not appear anywhere
    - `MKOverlayRenderer` -- should not appear anywhere
    - `UIViewRepresentable` in HFMapView.swift -- should not appear
  </action>
  <verify>
    <automated>cd /Users/haocheng_mini/Documents/projects/HouseFriend && xcodebuild build -project HouseFriend.xcodeproj -scheme HouseFriend -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f HouseFriend/Views/CrimeTileOverlay.swift` passes (file deleted)
    - `test ! -f HouseFriend/Views/NoiseSmokeRenderer.swift` passes (file deleted)
    - `grep -rc "NoiseSmokeRenderer" HouseFriend/` returns 0
    - `grep -rc "CrimeTileOverlay" HouseFriend/` returns 0
    - `grep -rc "MKTileOverlay" HouseFriend/` returns 0
    - `grep -rc "MKOverlayRenderer" HouseFriend/` returns 0
    - `grep -c "UIViewRepresentable" HouseFriend/Views/HFMapView.swift` returns 0
    - `grep -c "CrimeHotspot" HouseFriend/Services/CrimeService.swift` returns at least 1
    - `grep -c "import MapKit" HouseFriend/Views/HFMapView.swift` returns 0
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>Old renderer files deleted, Hotspot type moved to CrimeService, all MapKit overlay references removed from HFMapView, project builds clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 10 layers on Mapbox map</name>
  <files>none</files>
  <action>
    Human visually verifies all 10 layers on the Mapbox map by running the app on iOS Simulator. No code changes in this task -- purely verification of the completed migration.
  </action>
  <what-built>
    Complete Mapbox migration: all 10 data layers now render through Mapbox Maps SDK instead of Apple MapKit. The map uses dark tiles for crime, GPU-accelerated heatmap, stacked blur lines for noise, and Mapbox annotations for all pin types.
  </what-built>
  <how-to-verify>
    Build and run the app on the iOS Simulator (iPhone 16).

    1. **Map renders:** App launches and shows a Mapbox map centered on Bay Area (not a blank screen or Apple Maps)
    2. **Population layer (default):** 445 ZIP polygons visible with yellow borders and ZIP code text labels. Tap a ZIP -- it highlights in pink and shows demographics sheet.
    3. **Crime layer:** Switch to Crime -- map tiles go dark (night mode). Heatmap glow appears over SF/Oakland areas with real incident data. Toggle Details ON -- numbered cluster markers appear over the heatmap.
    4. **Noise layer:** Switch to Noise -- roads show smoke/haze effect. Freeway/highway lines should have visible blur aura. Pan to trigger Overpass fetch for detail streets.
    5. **Fire layer:** Switch to Fire Risk -- colored polygons (red/orange/yellow) over fire hazard zones.
    6. **Electric layer:** Switch to Power Lines -- yellow polylines for transmission lines.
    7. **Schools layer:** Switch to Schools -- graduation cap pins appear (green/blue/purple by level). Tap a school pin -- detail sheet opens.
    8. **Superfund layer:** Switch to Superfund -- orange warning triangle pins. Tap one -- detail sheet opens.
    9. **Earthquake layer:** Switch to Earthquake -- magnitude number markers (red/orange/yellow by severity).
    10. **Housing layer:** Switch to Group Homes -- teal house pins. Tap one -- detail sheet opens.
    11. **Air Quality layer:** Switch to Air Quality -- brown polygons for odor zones.
    12. **Gestures:** Long-press anywhere -- pin drops, neighborhood report panel appears with scores.
    13. **Style switching:** Switch between crime and any other layer -- map should go dark for crime, light for everything else.
    14. **Zoom filtering:** Zoom out to state level -- most annotation pins disappear. Zoom back in -- they reappear progressively.
    15. **User location:** Blue puck shows current location (if location permission granted).

    Report any layer that does not render, has wrong colors, or has broken interactions.
  </how-to-verify>
  <verify>Human visual verification -- no automated test possible for rendering correctness</verify>
  <done>All 10 layers visually confirmed working on Mapbox map by human reviewer</done>
  <resume-signal>Type "approved" if all 10 layers render correctly, or describe which layers have issues</resume-signal>
</task>

</tasks>

<verification>
1. Project builds with zero errors and zero references to deleted files
2. All 10 layers visually confirmed on simulator
3. Gesture handling works (tap, long-press)
4. Crime layer dark mode works
5. ZIP polygon tap/highlight works
6. School/superfund/housing taps open detail sheets
7. Crime cluster markers toggle with Details switch
</verification>

<success_criteria>
- Zero references to CrimeTileOverlay, NoiseSmokeRenderer, MKTileOverlay, MKOverlayRenderer in codebase
- CrimeTileOverlay.swift and NoiseSmokeRenderer.swift deleted
- All 10 layers render correctly on Mapbox map (visual verification)
- All gesture callbacks work end-to-end
- User approves visual verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-mapbox-migration/02.1-04-SUMMARY.md`
</output>
